#include<stdio.h>
int main()
{
    // relation opretors
    int i=1<2;
    printf("%d\n",i);
    int j = 10 < 1;
    printf("%d\n",j);
    return 0;
}
// ture = 1 and false = 0 
// < -- less than sign
// > -- greter than sign 
// == -- equal  to sign 
// != -- Not equal to 
// <= -- less than or equal to 
// >= -- greater than or equal to 
